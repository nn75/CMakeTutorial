# Step3
## Adding Usage Requirements for Library

1. INTERFACE means things that consumers require but the producer doesn't.
2. The primary commands that leverage usage  requirements are:
```
target_compile_definitions()
target_compile_options()
target_include_directories()
target_link_libraries()
```

